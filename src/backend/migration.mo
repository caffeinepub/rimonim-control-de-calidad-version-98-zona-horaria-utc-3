module {
    public type OldActor = { // current state
    };
    public type NewActor = { // migrated state
    };

    public func run(old : OldActor) : NewActor {
        // Perform migration steps as needed in the future
        {};
    };
};
